type User {
  id: ID! @unique
  username: String! @unique
  jwt: String
}

type Foo {
  id: ID! @unique
  name: String
  secretFoo: String @default(value: "i r secret")
}

type Route {
  id: ID! @unique
  type: String!
  path: String!
  description: String
  public: Boolean
}
